import 'package:flutter/material.dart';

class CommentTab extends StatelessWidget {
  const CommentTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Comment Tab'),
    );
  }
}
